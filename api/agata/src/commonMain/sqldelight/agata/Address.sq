import cz.lastaapps.api.core.domain.model.common.LatLong;

CREATE TABLE AddressEntity(
    id INTEGER NOT NULL,
    subsystemId INTEGER NOT NULL,
    address TEXT NOT NULL,
    gps TEXT AS LatLong NOT NULL,
    PRIMARY KEY (id)
);

getForSubsystem:
    SELECT * FROM AddressEntity WHERE subsystemId = :subsystemId;

insert:
    INSERT OR REPLACE INTO AddressEntity
    VALUES ?;

deleteAll:
    DELETE FROM AddressEntity;
