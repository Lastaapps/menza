CREATE TABLE ServingPlaceEntity(
    id INTEGER NOT NULL,
    name TEXT NOT NULL,
    description TEXT NOT NULL,
    abbrev TEXT NOT NULL,
    PRIMARY KEY (id)
);

getById:
    SELECT * FROM ServingPlaceEntity WHERE id = :id;

getByIds:
    SELECT * FROM ServingPlaceEntity WHERE id IN :id;

insert:
    INSERT INTO ServingPlaceEntity(id, name, description, abbrev)
    VALUES (:id, :name, :description, :abbrev);

insertEntity:
    INSERT INTO ServingPlaceEntity(id, name, description, abbrev)
    VALUES ?;

deleteAll:
    DELETE FROM ServingPlaceEntity;
